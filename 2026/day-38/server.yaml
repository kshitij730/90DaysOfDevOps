---
server:
  name: web-server
  ip: 192.168.1.10
  port: 8080

database:
  host: localhost
  name: devops_db
  credentials:
    user: admin
    password: admin123

startup_script_pipe: |
  sudo apt update
  sudo apt install nginx -y
  sudo systemctl start nginx

startup_script_fold: >
  sudo apt update
  sudo apt install nginx -y
  sudo systemctl start nginx
